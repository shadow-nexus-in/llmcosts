# Cursor IDE Rules — Cost-Optimized Configuration
# Generated by LLMCosts.dev — https://llmcosts.dev
# Updated: 2026-07-26
# Cheapest coding model as of today: Google: Lyria 3 Pro Preview ($0.0/$0.0 per 1M tokens)
# Route via: https://llmcosts.dev/out/api

[rules]
model = openrouter/google/lyria-3-pro-preview
api_base = https://llmcosts.dev/out/api

# AI-Generated Optimization Tips:
Here are 5 practical Cursor IDE rules for using Google: Lyria 3 Pro Preview efficiently for coding, focusing on prompt engineering tips to minimize token usage and cost:

**Rule 1: Specify the programming language and task**
```python
# Rule 1: Specify the programming language and task
def get_code_snippet(prompt):
    # Prepend the language and task to the prompt
    prompt = f"Write a {prompt['language']} code snippet to {prompt['task']}. "
    return prompt
```
Example: Instead of "Write a function to sort an array", use "Write a Python code snippet to sort an array using quicksort algorithm".

**Rule 2: Provide context and constraints**
```python
# Rule 2: Provide context and constraints
def add_context(prompt):
    # Add context and constraints to the prompt
    prompt += f" Assuming {prompt['assumptions']}, and the input is {prompt['input_type']}. "
    return prompt
```
Example: "Write a Python code snippet to sort an array using quicksort algorithm, assuming the input is a list of integers, and the output should be a sorted list in ascending order".

**Rule 3: Use concise and specific prompts**
```python
# Rule 3: Use concise and specific prompts
def simplify_prompt(prompt):
    # Remove unnecessary words and phrases
    prompt = prompt.replace("using", "").replace("with", "").replace("and", "")
    return prompt
```
Example: Instead of "Write a Python code snippet to sort an array using quicksort algorithm with a time complexity of O(n log n)", use "Python quicksort array sort".

**Rule 4: Break down complex tasks into smaller ones**
```python
# Rule 4: Break down complex tasks into smaller ones
def break_down_task(prompt):
    # Split the prompt into smaller sub-tasks
    sub_tasks = prompt.split(",")
    prompts =

# Cost Tip: Enable caching in your IDE settings to cut input costs by ~50%.
# See full comparison: https://llmcosts.dev/models/lyria-3-pro-preview
