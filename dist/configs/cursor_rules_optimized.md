# Cursor IDE Rules — Cost-Optimized Configuration
# Generated by LLMCosts.dev — https://llmcosts.dev
# Updated: 2026-08-11
# Cheapest coding model as of today: Google: Lyria 3 Pro Preview ($0.0/$0.0 per 1M tokens)
# Route via: https://llmcosts.dev/out/api

[rules]
model = openrouter/google/lyria-3-pro-preview
api_base = https://llmcosts.dev/out/api

# AI-Generated Optimization Tips:
### Cursor IDE Rules for Efficient Google Lyria 3 Pro Preview Usage

To optimize the use of Google Lyria 3 Pro Preview for coding while minimizing token usage and cost, consider implementing the following Cursor IDE rules. These rules are designed with prompt engineering tips to ensure efficient and cost-effective coding assistance.

#### Rule 1: **Specific Function Queries**
```python
# Rule to prompt for specific function implementations
if cursor_context.contains("function") and cursor_context.contains("implementation"):
    prompt = f"Implement a {cursor_context['function']} function in {cursor_context['language']}."
    # Use the cheapest model appropriate for the task, e.g., a code-specific model
    model = "code-davinci-002"
    # Limit the response to minimize tokens used
    max_tokens = 128
    # Execute the query
    response = lyria_query(prompt, model, max_tokens)
    # Insert the response at the cursor position
    cursor.insert_text(response)
```

#### Rule 2: **Code Completion with Context**
```python
# Rule for code completion based on the current file context
if cursor_context.contains("code_context"):
    prompt = f"Complete the code based on the context: {cursor_context['code_context']}."
    # Utilize a model that excels in code understanding and completion
    model = "code-cushman-001"
    # Adjust max_tokens based on the complexity of the context
    max_tokens = 64
    # Execute the query
    response = lyria_query(prompt, model, max_tokens)
    # Suggest the completion
    cursor.suggest_text(response)
```

#### Rule 3: **Debugging Assistance**
```python
# Rule to assist in debugging code
if cursor_context.contains("error_message"):
    prompt = f"Debug the error: {cursor_context['error_message']}. Provide step-by-step analysis and fix."
    # Employ a model capable

# Cost Tip: Enable caching in your IDE settings to cut input costs by ~50%.
# See full comparison: https://llmcosts.dev/models/lyria-3-pro-preview
